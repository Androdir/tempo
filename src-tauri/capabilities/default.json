{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window. No network, shell, or filesystem plugins are enabled.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
